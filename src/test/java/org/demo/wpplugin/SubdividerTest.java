package org.demo.wpplugin;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SubdividerTest {
    /*
    @Test
    public void subdivide() {
        float[] xs = new float[]{ 100, 200};
        {
            float[] xsSub = Subdivide.subdivide(xs, 0, 1, new HalfWaySubdivider(0,0,false));
            assertArrayEquals(new float[]{100, 150, 200}, xsSub);
        }
        {
            float[] xsSub = Subdivide.subdivide(xs, 0, 2, new HalfWaySubdivider(0,0,false));
            assertArrayEquals(new float[]{100, 125, 150, 175, 200}, xsSub);
        }
        {
            float[] xsSub = Subdivide.subdivide(xs, 0, 3, new HalfWaySubdivider(0,0,false));
            assertArrayEquals(new float[]{100,112.5f, 125, 137.5f, 150, 162.5f, 175, 187.5f, 200}, xsSub);
        }
    } */
}